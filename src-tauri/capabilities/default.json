{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for LNPM desktop windows",
  "windows": ["main", "popup"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
